import Link from 'next/link'
import { requireStaff } from '@/lib/auth/guards'

type Props={searchParams:Promise<{status?:string}>}
export default async function CockpitDocumentsPage({searchParams}:Props){const {status=''}=await searchParams;const {supabase}=await requireStaff();let query=supabase.from('documents').select('id,case_id,file_name,mime_type,size_bytes,sensitivity,review_status,created_at').order('created_at',{ascending:false}).limit(150);if(['pending','review','needs_info','approved','rejected'].includes(status))query=query.eq('review_status',status);const {data:docs}=await query;return <main className="p-5 sm:p-8 lg:p-10"><h1 className="font-display text-4xl font-black">מסמכים</h1><form className="mt-5 flex gap-2"><label className="sr-only" htmlFor="doc-status">סינון סטטוס</label><select id="doc-status" name="status" defaultValue={status} className="rounded-xl border border-white/15 bg-[#07111f] px-4 py-3"><option value="">הכל</option>{['pending','review','needs_info','approved','rejected'].map(x=><option key={x}>{x}</option>)}</select><button className="rounded-xl bg-[#ef8c2f] px-5 font-black text-[#111827]">סינון</button></form><section className="mt-6 overflow-hidden rounded-[24px] border border-white/10 bg-white/5"><div className="divide-y divide-white/10">{docs?.length?docs.map(doc=><Link key={doc.id} href={`/cockpit/cases/${doc.case_id}`} className="grid gap-2 px-5 py-4 md:grid-cols-[1fr_150px_140px]"><strong className="truncate">{doc.file_name}</strong><span className="text-xs text-slate-400">{doc.sensitivity}</span><span className="text-xs font-bold text-[#ef8c2f]">{doc.review_status}</span></Link>):<p className="p-8 text-slate-400">אין מסמכים.</p>}</div></section></main>}
